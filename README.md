# Тестовое задания для AITI Guru

Frontend-приложение на **React + TypeScript**, реализующее авторизацию, работу со списком товаров и поиск.  
Проект построен с использованием **feature-based архитектуры** и ориентирован на масштабируемость и читаемость кода.

---

## 🚀 Технологии

- **React 19**
- **TypeScript**
- **Vite**
- **React Router**
- **@tanstack/react-query** — работа с серверным состоянием
- **Axios**
- **React Hook Form**
- **SCSS Modules**
- **pnpm** — менеджер пакетов
- **ESLint + Prettier**
- **Husky**
- **vite-plugin-svgr** — для импорта SVG как компонентов

---

### 🧠 Управление состоянием

В этом проекте глобальный state-менеджер специально не используется.

Обычно в рабочих проектах я применяю Redux Toolkit или Zustand,
но здесь это избыточно, так как:

основное состояние — серверное

объём локального состояния минимален

React Query полностью покрывает нужды управления состоянием данных

## 📦 Менеджер пакетов

В проекте используется **pnpm** для установки зависимостей и управления пакетами.  
Преимущества pnpm:

- Быстрая установка зависимостей
- Экономия дискового пространства
- Строгая работа с версиями зависимостей

> Установка и запуск команд рекомендуется выполнять через `pnpm`.

---

## 📥 Установка и запуск

### Установка зависимостей

```bash
pnpm install
```

### Запуск в режиме разработки

```bash
pnpm dev
```

### Сборка проекта

```bash
pnpm build
```

### Предпросмотр production-сборки

```bash
pnpm preview
```

### Проверка кода и форматирование

````bash
pnpm lint # проверка eslint
pnpm lint:fix # автоисправление ошибок eslint
pnpm format # форматирование prettier
pnpm type:check # проверка типов TypeScript
```bash
````

## 🧱 Архитектура проекта

Проект построен по принципам Feature-Sliced Design с разделением на:

- app — инициализация приложения, провайдеры, глобальные стили

- pages — страницы приложения

- features — бизнес-фичи (auth, product, search)

- shared — переиспользуемые UI-компоненты, утилиты, API, конфиги и типы

### 📂 Структура проекта (кратко)

- src/app — entry point, layouts, провайдеры, глобальные стили

- src/pages — страницы:

- login — авторизация

- products — список товаров с таблицей и сортировкой

- not-found-page — 404

- src/features — фичи:

- auth — авторизация, токены, защищённые и гостевые маршруты

- product — добавление продукта, API, формы

- search — поиск и хук useSearch

- src/shared — переиспользуемое:

- api — axios, react-query client

- components — UI-компоненты (Button, Input, Modal, Spinner и др.)

- config — роуты, query-keys, env

- model — типы и интерфейсы

- icons — SVG-иконки

## 🔐 Авторизация

- Через API

- Поддержка защищённых и публичных маршрутов

- Layout’ы для авторизованных и неавторизованных пользователей

## 📊 Работа с данными

- Все запросы через React Query

- Отображается состояние загрузки и ошибки

- Централизованный Axios-инстанс для API

## 🧩 UI и стили

- SCSS Modules

- Компоненты инкапсулируют стили и логику

- Иконки подключаются через `vite-plugin-svgr`

## 🛠️ Дополнительно

- ESLint + Prettier для поддержания качества кода

- Husky для pre-commit хуков

- Типизация на уровне API и форм

- Feature-based структура облегчает масштабирование приложения
